/*
  Google OAuth configuration.

  GOOGLE_CLIENT_ID is public configuration and may be used in browser code.
  Never put GOOGLE_CLIENT_SECRET, service-account keys, or private API keys here.
  For production, set apiBaseUrl to your backend that verifies the Google ID token.
*/
window.BLUE_PENGUIN_AUTH_CONFIG = {
  googleClientId: 'PASTE_YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com',
  apiBaseUrl: '',
};
